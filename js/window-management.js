const { ipcRenderer } = require("electron");

const closeWindow = () => {
//       location.close()
}

const refreshWindow = () => {
       location.reload();
}

const reframeMainWindow = () => {
      //  remote.mainWindow({frame: true})
}
